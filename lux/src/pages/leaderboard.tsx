import {HomeTemplate} from "@shared/ui";

export const LeaderboardPage: React.FC = () => {
    return (
        <HomeTemplate>
            <div>Leaderboard page</div>
        </HomeTemplate>
    );
};
